{
  "name": "city-kit",
  "version": "0.0.1",
  "description": "Fork of citysdk focused on clean api only",
  "scripts": {
    "prestart": "npm run build",
    "start": "node lib/api/main.js",
    "build": "babel src -d lib"
  },
  "dependencies": {
    "basic-auth": "^2.0.0",
    "body-parser": "^1.18.3",
    "express": "^4.16.3",
    "request": "^2.88.0",
    "terraformer": "^1.0.9",
    "terraformer-arcgis-parser": "^1.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.0.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
